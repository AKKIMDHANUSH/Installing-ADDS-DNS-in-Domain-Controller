# Installing-ADDS-DNS-in-Domain-Controller
Install ADDS &amp; DNS in Domain controller, Add Clients to that Domain Controller and create users. The users should also access to folders with other machines.
